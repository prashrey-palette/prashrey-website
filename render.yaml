# Render static site configuration
# Docs: https://render.com/docs/static-sites

services:
  - type: web
    name: prashreypalette
    runtime: static
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
