export type Artwork = {
  title: string;
  category: string;
  image: string;
};

export type Stat = {
  value: string;
  label: string;
};

export type NavLink = {
  href: string;
  label: string;
};
